# Background music

The active track is `marry-me.m4a`, configured through `music.src` in
`config/wedding.ts`. The player loops and starts immediately after the guest clicks
the **Buka Undangan** button, which keeps playback compatible with mobile browser
autoplay policies.
